using EventBTree;

public static class SubtreeFactory
{
    public static Composite GetProtectSubtree(BlackBoard blackBoard)
    {
        //get all the threats
        //position between the target and the threats
        var root = new Selector();
        return root;
    }
    public static Composite GetHuntSubtree(BlackBoard blackBoard)
    {
        var root = new Selector();
        return root;
    }
}
